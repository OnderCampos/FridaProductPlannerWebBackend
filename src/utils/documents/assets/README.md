Place the DOCX header image here as `header.png`.

The document generator (`src/utils/documents/user_story_document.py`) will embed it into the header if present.

Alternative: set an absolute path via the environment variable `USER_STORY_DOC_HEADER_IMAGE`.
